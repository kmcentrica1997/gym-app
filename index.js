/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Profile from './src/home/screens/Profile';

AppRegistry.registerComponent(appName, () => Profile);
